import { StyleSheet, Text } from "react-native";

export default function Principal() {
  return <Text>Tela principal</Text>;
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },
});
